#Область СлужебныеПроцедурыИФункции



Процедура ОбработкаПолученияПредставления(Представление, ДанныеСтроки, СтандартнаяОбработка) 
    
    Если ЗначениеЗаполнено(ДействуетНаОсновании) Тогда
        СтандартнаяОбработка = Ложь;
        Представление = ФИО + " (" + ДействуетНаОсновании + ")";
    КонецЕсли;
    
КонецПроцедуры

#КонецОбласти
